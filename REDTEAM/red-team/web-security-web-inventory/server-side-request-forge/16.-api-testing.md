# 16. API Testing

## API Testing Notes

### Application Programming Language

* API is not a programming language. It is a set of rules that allows different software application to communicate with each other.

### API Testing

* API Testing involves finding vulnerabilities like SQL injection which can affect how APIs handle and transmit data.

### API Recon

* Gathers as much information as possible about APIs such as endpoints, how they receive and handle requests, and where they are located on the server.

### Discovering the API

* Some API documentation is machine-readable, providing instruction on how to use and integrate the API.
* There are API scanners that can be used.

Examples are:

* `/api`
* `/openapi.json`

If you find an endpoint with a resource, try to investigate the path like `/api/servicename/v1/users/123`, then try:

* `/api/servicename/v1`
* `/api/servicename/`
* `/api`

Automate the process using OPENAPI parser and scanner like BWAP Scanner:

* https://portswigger.net/bappstore/6bf7574b632847faaaa4eb5e42f1757c

### Find the API Endpoints

* By browsing the web application there is information on API like `/api/`.
* Also look for JavaScript, and for more heavy wait extraction use JS Link Finder.

BApp link:

* https://portswigger.net/bappstore/0e61c786db0c4ac787a08c4516d52ccf

### Interacting the Endpoint

* Once you find the endpoint, you can interact with it using Burp Repeater and Intruder. This helps to discover attack surface like changing the HTTP or media type.

### Identifying HTTP Methods

GET > Is get data from the resources.

PATCH > Applies partial changes.

OPTIONS >> GET information on the type of request method used on the resources.

* Endpoints support different HTTP methods, therefore it is important to check all the HTTP methods and investigate API endpoints.

Example:

* `api/task/`
* `GET /api/tasks` >> get the data
* `POST /api/tasks` >> create new task
* `DELETE /api/tasks/1` delete task.

### Find Supported Content-Type

* API use specific data format, and they have behave differently depending on the content-type of data provided in request.
* Changing the content-type may:
  * Trigger error which discloses useful information.
  * Use advantage of different in process in logic: an API is secure handling JSON but susceptible (effected) when dealing with XML.
* To do that change the content-type header and use content-type converter BAPP link:
* https://portswigger.net/bappstore/db57ecbe2cb7446292a94aa6181c9278

### Testing Mass Assignments

* It finds the hidden parameter in the JSON Data by manually finding object return by API.
* Means you have request `GET /api/users/123` where its response is JSON Data:

```json
{
  "id": 123,
  "name": "John Doe",
  "email": "john@example.com",
  "isAdmin": "false"
}
```

* Change the isadmin parameter to something else and see the response.
* It may give error, so change to True:

```json
{
  "username": "wiener",
  "email": "wiener@example.com",
  "isAdmin": true
}
```

* If this request is connected to isAdmin without any security, then the wiener user may get admin privileges.

### Prevent API Vulnerabilities

* Securely document the API, don't publish it.
* Apply and access list of HTTP method to be permitted.
* Validate that the content-type is requested in every request.
* Use generic error so the attacker can’t use this information.

***

## API Testing Portswigger

### Exploiting an API endpoint using documentation

Map the application API structure and endpoints

Home Page

These are the HTML resources from which various elements are loaded.

Discovery the endpoint.

* Identify how data is fetched from the backend by refreshing the page and monitoring network activity.
* There are two requests in the HTML document, indicating that the data is being fetched from the backend.

Logged in as the user 'wiener' with the password 'peter'

* After logging in, there are endpoints containing the word 'APIs'
* There is a request using the FETCH API that updates the email via the PATCH method.
* ChatGPT Explanation
* Change the email and check how the API handles the update.
* Found the API which is updating EMAIL.

Testing API Endpoints.

* Send the request to Burp Suite Repeater for further analysis
* It uses the PATCH method to update the email address.
* Remove the session cookie to test for proper session validation and authorization checks.
* It's unauthorized, indicating that the session is properly protected.
* Using the wiener username update email request and changing the username
* There is a bug causing information disclosure while viewing the 'wiener' account, allowing access to sensitive data.
* Inspect the directory structure for additional API endpoints or sensitive information.
* GET /api/ shows the HTTP methods used by the API to communicate with the backend.
* Attempt to delete the 'Carlos' user by sending a DELETE request to the relevant API endpoint.
* Proof Value

***

## Exploiting server-side parameter pollution in a query string

* Map the application's structure and identify potential vulnerabilities
* HOME PAGE
* HTTP history analysis shows no accessible endpoints without logging in.
* Discovered the 'forgot password' JavaScript functionality
* Logged in using the credentials 'wiener

### HTTP History

* Review the HTTP history to analyze interactions after login
* Identified an interesting behavior in the HTTP requests.

### Forgotpwdready

* The window.location.search property retrieves the query string from the current URL.
* The URLSearchParams object allows you to work with the query string of a URL, particularly for parsing and handling parameters.
* And then there is `/forgot-password?reset_token$` >> which redirect me to new page
* Testing the 'forgot password' endpoint.
* Packet
* Testing the forgot password feature using valid credentials.
* Used the OPTIONS method to identify the available HTTP methods supported by the 'forgot-password' endpoint.
* GET
* POST with administrator username
* It giving the error to “Field not specified” with # also didn’t work
* Tried 'Administrator' with URL-encoded '#' and a new 'username' parameter.
* The first parameter, 'username', is ignored, and the crafted second parameter, created using URL-encoded '#', bypasses security checks. This allows unauthorized access to the 'administrator' username's email information
* Final Crafted request
* Brute-force the query parameters to discover hidden field names
* Sending to intruder
* Adding into brute force
* Normal response
* Payload set
* Results
* Response
* Test the discovered field to check for further vulnerabilities.
* A bug in the 'wiener' account allows unauthorized information disclosure, enabling viewing of sensitive data.
* Password parameter not avaible
* Passwd parameter not avaiable
* Checking the valid parameter which i know is email
* Checking the valid parameter username
* The 'reset\_token' parameter can be exploited to generate a session token, allowing direct login to the administrator account
* Got the rest token
* Login with the URL
* Creating administrator new password
* Packet
* Update administrator password.
* Loggin in as administartor account.
* Packet
* My account
* Deleting user carlos

***

## Finding and exploiting an unused API endpoint

Map the application.

Home Page: Analyze the home page and identify available products.

Lightweight Jacket Product: Investigated the product and interaction.

Unique Message: Identified a unique message when interacting with the product.

Packets: Analyzed packets during the interaction.

Observations:

* Balance: Noticed that the user balance shows "zero dollars."
* Basket Details: Viewed the details of the product added to the basket.

Discovering the Endpoint:

Adding to cart

Cart

* Endpoint Found: Discovered `/api/product/1/price` during testing.
* Burp Repeater: Sent the request to Burp Repeater for further testing

Directory Discovery:

* Tried accessing various endpoints such as:
  * `/api/product/1/`
  * `/api/product/1`
  * `/api/products/`
  * `/api/product`
  * `/api/`
  * `/api`

`/api/product/1/`

`/api/product/1`

`/api/products/`

`/api/product`

`/api/`

`/api`

Outcome: No significant results found, except for `/api/product/1/price`.

Testing API Endpoints:

`/api/products/1/price`

Changed the HTTP method from POST to GET to test responses.

Using the PATCH Method:

* Initially encountered an unauthorized error.

Logged in as 'wiener': Accessed the cart and tried various actions.

Cart

Adding to cart

* Sent the product endpoint to Burp Repeater again.

Testing Methods:

Normal GET Request: Observed the regular response.

POST and PATCH Methods: Attempted to modify the request using PATCH.

PATCH Methods

Encountered an error: "Only 'application/json' Content-Type is supported."

Added an empty JSON payload `{}` but the error persisted.

Still the same error

Adding Proper Headers and Body:

Set the Content-Type header to application/json.

New error: "Price parameter missing in body."

Added the price parameter but got the error: "Price parameter must be a valid non-negative integer."

Exploitation:

* Set the price parameter to zero (valid integer).
* Successfully changed the product price to zero for the jacket.

The jacket product price is zero

Cart

Results

Repeated the process for another product using `/api/product/2/price`.

`/api/product/2/price`

Sending to burp reapter

Changed the price of the second product to zero as well.

The price is changed.

Results

Final Steps:

* Added the products with zero price to the cart.

Checkout: Completed the checkout process with the manipulated prices.

Solved.

***

## Exploiting a mass assignment

Map the application

Lab: Begin by exploring the lab environment.

Home Page: Start from the home page to identify key interactions.

Endpoint Discovery:

Logged in as user 'wiener' to examine functionality and access restricted areas.

Testing the API Endpoint

Checked the available products, such as the jacket product.

Using Burp Suite, reviewed the history and sent relevant packets to the Repeater for further analysis.

Discovered the `/api/checkout` endpoint during testing.

Handling Errors:

Initially encountered an insufficient privilege error when attempting to interact with the checkout endpoint.

Sent the request to Burp Repeater for testing different HTTP methods and parameters.

Parameters

Using GET and POST Methods:

POST Methods

Tested both GET and POST methods to explore how the API handled requests.

POST Method: Attempted to apply changes to the product.

Mass Assignment Exploit:

* Discovered a discount parameter in the JSON data returned by the API.

Copy the discount json parameter

Added this parameter to the checkout request to manipulate the price of the item.

Applied various discounts

10% discount: Successfully reduced the price.

200% discount: Tested for extreme reductions, revealing a flaw in how the discount was applied.

100% discount: Final manipulation allowed the item to be obtained for free.

Solved

Results
